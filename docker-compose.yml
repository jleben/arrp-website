version: "3"
services:
    ui:
        image: jakobleben/arrp-web-ui:eef9147
        networks:
            - traefik-net
        deploy:
            replicas: 1
            restart_policy:
                condition: on-failure
            labels:
                traefik.frontend.rule: "Host:arrp-lang.info"
                traefik.port: 80
                traefik.docker.network: traefik-net
    engine:
        image: jakobleben/arrp-web-engine:0f67eb8
        networks:
            - traefik-net
        deploy:
            replicas: 1
            restart_policy:
                condition: on-failure
            labels:
                traefik.frontend.rule: "Host:arrp-lang.info;PathStrip:/playground/request"
                traefik.port: 8000
                traefik.docker.network: traefik-net

networks:
    traefik-net:
        external: true
